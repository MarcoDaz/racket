<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>title </title>
</head>
<body>
    
    <% if (product) { %>
        <h1><%= product.name %> </h1>
        <h1>current price: <%= product.prices.at(-1).price %> </h1>
            <h3>Added: <%= product.prices.at(-1).date %> </h3>
            
            <h3>This was priced lowest at: <%= lowestPrice.price %> on <%= lowestPrice.date %> </h3>
            <h3>... and was priced highest at: <%= highestPrice.price %> on <%= highestPrice.date %> </h3>

            <canvas id="<%= product._id %>"> </canvas>
            <script src="/javascripts/chart.js"></script>
            <script src="/javascripts/applyChart.js"></script>
                <script>
                    const product = <%- JSON.stringify(product) %> 
                    applyChart(product)
                </script>
                
    <% } else { %>
        <h1>not found</h1>
    <% } %>
    
</body>
</html>